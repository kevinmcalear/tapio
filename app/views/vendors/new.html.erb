<% unless @vendor.nil? || @vendor.id.nil? %>
  <div id="errors">
    <h3>Errors!</h3>
    <ul>
      <% @vendor.errors.messages.each do |field,error| %>
        <li><em><%= "#{field.capitalize} #{error.first}!" %></em></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= render "form" %>
